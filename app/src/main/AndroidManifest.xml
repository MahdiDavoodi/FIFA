<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="davoodi.mahdi.fifa">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/appName"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FIFA">
        <activity android:name=".pages.SelectClubsPage"></activity>
        <activity android:name=".pages.CreateOwnersPage" />
        <activity android:name=".pages.StartPage" />
        <activity android:name=".pages.LoadingPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".pages.MainPage" />
    </application>

</manifest>