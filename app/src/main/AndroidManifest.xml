<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="davoodi.mahdi.oufa">

    <application
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/appName"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FIFA">
        <activity android:name="davoodi.mahdi.oufa.pages.TransferHistoryPage" />
        <activity android:name="davoodi.mahdi.oufa.pages.NewTransferPage" />
        <activity android:name="davoodi.mahdi.oufa.pages.StatisticsPage" />
        <activity android:name="davoodi.mahdi.oufa.pages.PlayPage" />
        <activity android:name="davoodi.mahdi.oufa.pages.InfoPage" />
        <activity android:name="davoodi.mahdi.oufa.pages.SlidesPage" />
        <activity android:name="davoodi.mahdi.oufa.pages.SelectClubsPage" />
        <activity android:name="davoodi.mahdi.oufa.pages.CreateOwnersPage" />
        <activity android:name="davoodi.mahdi.oufa.pages.StartPage" />
        <activity android:name="davoodi.mahdi.oufa.pages.LoadingPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="davoodi.mahdi.oufa.pages.MainPage" />
    </application>

</manifest>